<h2> Reminder List </h2>

<ul>
  {{#each model as |reminder|}}
    <li class="spec-reminder-item">
      <div>{{#link-to 'reminders.reminder' reminder.id}} {{reminder.title}} {{/link-to}}</div>
      <div>Date: {{reminder.date}}</div>
    </li>
  {{/each}}
</ul>

{{outlet}}
